machine:
  node:
    version: 4.1.1
  environment:
    S3_BUCKET: redsift-static

checkout:
  post:
    - git fetch --tags
    - npm install
  pre:
    - sudo pip install awscli

dependencies:
  override:
    - node node_modules/gulp/bin/gulp.js
    - mv ./dist ${CIRCLE_ARTIFACTS}/dist

test:
  override:
    - /bin/bash -c "[ -s ${CIRCLE_ARTIFACTS}/dist/css/ui-rs-hero.min.css ]"
    - /bin/bash -c "[ -s ${CIRCLE_ARTIFACTS}/dist/js/ui-rs-hero.umd-es2015.min.js ]"

deployment:
  production:
    branch: /release-.*/
    commands:
      - echo "Uploading to S3 bucket..."
      - aws s3 cp ${CIRCLE_ARTIFACTS}/dist s3://$S3_BUCKET/reusable/latest --recursive --acl public-read
      - |
         VERSION=$(git describe --exact-match --tags 2>/dev/null || git rev-parse --short HEAD)
         aws s3 cp ${CIRCLE_ARTIFACTS}/dist s3://$S3_BUCKET/reusable/$VERSION --recursive --acl public-read
      - echo "S3 upload complete."
